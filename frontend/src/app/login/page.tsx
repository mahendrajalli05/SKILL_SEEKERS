export default function LoginPage() {
  return (
    <div className="max-w-lg border border-[var(--line)] bg-white p-6">
      <h1 className="text-2xl font-semibold text-[var(--navy)]">Officer login</h1>
      <p className="mt-3 text-sm text-[var(--muted)]">
        Demo role authentication is not implemented in this foundation slice.
        The local prototype is open. Production-scale security is P2.
      </p>
    </div>
  );
}
