export default function SearchPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold text-[var(--navy)]">Search</h1>
      <p className="mt-2 max-w-2xl text-sm text-[var(--muted)]">
        Work search is scheduled after real MPLADS data is ingested and
        profiled. This page is a route shell only.
      </p>
    </div>
  );
}
